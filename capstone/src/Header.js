import logo from './little-lemon-logo.svg';

function Header() {
    return (
        <header>
            <img src={logo} alt="Little Lemon logo" />
        </header>
    );
}

export default Header;
